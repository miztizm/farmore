# Farmore Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit your .env file to version control!

# ============================================================================
# GitHub Authentication
# ============================================================================

# GitHub Personal Access Token (PAT)
# Required for: Private repositories, higher rate limits, organization repos
# Without token: 60 requests/hour (unauthenticated, public repos only)
# With token: 5,000 requests/hour (authenticated, public + private repos)
#
# ⭐ RECOMMENDED: Use Classic Personal Access Token
#
# How to create a Classic PAT:
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Tokens (classic)" → "Generate new token (classic)"
# 3. Give it a descriptive name: "farmore-backup"
# 4. Set expiration: 90 days recommended
# 5. Select scopes based on features you need:
#    ✅ 'repo' - REQUIRED for private repositories, starred/watched repos, secrets
#    ✅ 'delete_repo' - REQUIRED for repository deletion (use with caution!)
#    ✅ 'user' - OPTIONAL for detailed profile information
#    (Note: 'repo' includes: repo:status, repo_deployment, public_repo, repo:invite)
# 6. Click "Generate token"
# 7. Copy the token (starts with ghp_) and paste it below
#
# Why Classic PAT?
# - ✅ Simpler setup (just check 'repo' scope)
# - ✅ Proven to work with all repository types
# - ✅ Includes personal + organization repos automatically
# - ✅ Broader API compatibility
#
# Security best practices:
# - Never commit this token to version control (.env is in .gitignore)
# - Treat it like a password
# - Rotate tokens every 90 days
# - Revoke unused tokens at https://github.com/settings/tokens
#
GITHUB_TOKEN=ghp_your_token_here_replace_this_value

# ============================================================================
# Optional Configuration
# ============================================================================

# Default destination directory for backups
# FARMORE_DEST=./farmore_backups

# Default visibility filter (public, private, all)
# FARMORE_VISIBILITY=all

# Include forked repositories by default (true, false)
# FARMORE_INCLUDE_FORKS=false

# Include archived repositories by default (true, false)
# FARMORE_INCLUDE_ARCHIVED=false

# Number of parallel workers for cloning/updating
# FARMORE_MAX_WORKERS=5

# Use SSH for cloning by default (true, false)
# FARMORE_USE_SSH=true

# ============================================================================
# Usage
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env and add your GitHub token
#
# 3. Run farmore (it will automatically load .env):
#    farmore user octocat --dest ./backups
#
# Note: Command-line arguments override environment variables
#
# ============================================================================

# "Configuration is just organized secrets." — schema.cx

